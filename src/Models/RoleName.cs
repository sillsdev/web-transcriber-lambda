using System;

namespace SIL.Transcriber.Models
{
    public enum RoleName
    {
        SuperAdmin = 1,
        Admin = 2,
        Transcriber = 3, // default
        Reviewer = 4, 
        Member = 5
    }
}
